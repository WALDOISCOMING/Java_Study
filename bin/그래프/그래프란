그래프란
그래프는 G=(V,E)
V는 Vertex(정점) node라고도 한다.
E는 Edge(엣지) link라고ㅓ도 한다.
개체간의 이진 관계를 말한다.
무방향성 그래프는  u-v,와 v-u는 같은것으로 보고 하나만 셈.
n = |V| ,m=|E|
V={1,2,3,4,5,6,7,8}
E={(1,2),(1,3),...(7,8)}
n=8
m=11

방향그래프는u-v와 v-u사이 다르다고 본다.

가중치 그래프
엣시 E에 가중치가 지정된다.

그래프를 표현하는 방법.
1.인접 행렬.
	n*n행렬로 그래프를 가정한다. 서로 E Edge가 존재하면 1로 아니면 0이라고 한다.
	저장 공간은 n*n이며, 노드 V에 연결되어있는 Edge를 탐색하기 위한 시간은 O(n), 타겟 Edge 하나를 탐색하기 위한 시간은 O(1)이다. 
	무방향이면 대칭적이지만, 무방향이라면 비대칭적으로 되야한다.	
2.인접 리스트
	정점 집합을 표현하는 하나의 배열과 정점마다 인접한 정점들의 연결 리스트로 표현.
	저장 공간은 n+m. 노드 V에 연결되어있는 Edge탐색으로 모두다른 V 찾기O(degree(v)) 그 노드의 깊이., 타겟 엣지 존재하는지 검사O(degree(if 방향성?2*v:v)) 
			

가중치 그래프.
	엣지의 가중치를 저장한다.
	만약 가중치가 거리나 비용을 의미한다면 에지가 없다면 무한대 vertex는 0
	가중치가 용량을 의미한다면 엣지가 없으면 0,vertex는 무한대

노드 사이 연결 경로가 존재한다면 서로 연결되어있다고 한다.



	